{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "npx vite dist --port 5173",
    "preview": "npx vite dist --port 5173"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
